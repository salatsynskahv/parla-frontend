import { DecimalValidatorDirective } from './decimal-validator.directive';

describe('DecimalValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
