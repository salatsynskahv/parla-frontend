import { createReducer, on } from '@ngrx/store';
import { addItem } from './actions';
import { Term } from '../entities/vocabulary';




// export const state = createReducer(
//     on(addItem, (state, {term}) => ({...state, vocabulary: [...state.vocabulary, term]}))
// );

